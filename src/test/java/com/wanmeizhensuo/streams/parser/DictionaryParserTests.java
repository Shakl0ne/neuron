package com.wanmeizhensuo.streams.parser;

import io.vertx.core.json.Json;

import org.junit.Assert;
import org.junit.Test;

/*
public class DictionaryParserTests {
    @Test
    public void testSample0() throws Throwable {
        var data = Json.decodeValue("{\"source\":\"source0\", \"table\":\"table name\",\"on\":\"table.field.name\"}");
        var state = new StreamState(data);
        var parser = new DictionaryParser();
        System.out.println(parser.parse(state));
    }
}
*/
